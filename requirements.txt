# Ядро (обязательно) — работает и на ПК, и на Raspberry Pi
numpy>=1.24
opencv-python>=4.8.0      # на Raspberry Pi заменить на opencv-python-headless
Pillow>=10.0.0            # кириллические подписи
onnxruntime>=1.16.0       # модель эмоций (FER+)

# Опционально — детекция рук (--hands). На слабом Pi можно не ставить.
# mediapipe>=0.10.0

# Опционально — камера Raspberry Pi (--picamera).
# Ставится системно: sudo apt install -y python3-picamera2
